# ROM Custom GPT

Esta carpeta contiene los archivos que se suben al Custom GPT.

Objetivo:

```txt
definir cómo el agente entiende preguntas,
construye JSON para el backend,
llama /api/agent/router
y responde al usuario.
```

Regla:

```txt
ROM = instrucciones operativas del Custom GPT.
docs/ = documentación técnica del proyecto.
```

Archivos esperados:

```txt
query-builder.md
render.md
instrucciones.md
schema.openapi.yaml
```

Estado actual:

```txt
estructura creada
contenido pendiente de completar
```
